CONFIGS PARA SGA E PGA

SHOW PARAMETER SGA_TARGET
SHOW PARAMETER PGA_AGGREGATE_TARGET
SELECT VALUE/1048576 FROM V$PGASTAT WHERE NAME='maximum PGA allocated';
MEMORY_TARGET = SGA_TARGET + MAX(PGA_AGGREGATE_TARGET, MAXIMUM PGA ALLOCATED)
ALTER SYSTEM SET MEMORY_MAX_TARGET = n M ESCOPO = SPFILE;
memória_max_target = n M
alvo_memória = m M


VER MEMORIA 
select * from v$memory_target_advice order by memory_size;

EDITAR SGA FORA DOS PARAMETROS DE INICIALIZAÇÃO 

ALTER SYSTEM SET SGA_TARGET=value [SCOPE={SPFILE|MEMORY|BOTH}]
ALTER SYSTEM SET MEMORY_TARGET = 0;
